# Copy this file to .streamlit/secrets.toml (locally) or paste its contents
# into the "Secrets" box in Streamlit Community Cloud's app settings.
# Never commit your real secrets.toml to version control.

OPENAI_API_KEY = "sk-..."
